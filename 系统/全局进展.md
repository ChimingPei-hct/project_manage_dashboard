# PMD 全局进展

> 单一事实来源:当前做到哪里、下一步做什么。所有阶段推进必须先更新本文档,再动代码。

最后更新:2026-05-22

> **阶段重排(2026-05-22)**:原 Phase 6(飞书 OAuth)提前到 Phase 3 之前,因为后续所有功能需要在真实登录态下验收。原 Phase 3(状态填报编辑流)顺延为 Phase 4。

---

## 已完成

### Phase 0 — 文档体系(commit `29594d7`)
- 项目根 `CLAUDE.md` + harness 工作流
- `design/01`–`14` 共 14 篇约束文档(含 `03-代码与数据分离` 单独承载)
- `design/CLAUDE.md` / `design/_index.md` / `app/*/CLAUDE.md` 路由

### Phase 1 — 骨架(commit `a99ea94`, `9f128fd`)
- 后端 FastAPI 单文件 `main.py`:文件锁原子写、AuthMiddleware、Dev 后门、五角色判定、PDT/LTC/Module CRUD、status 双写、SSE、周快照
- 前端 Vue 3 + Vite:`?view=` 路由、4 个视图占位、StatusCell、TimelineBar、WeekSwitcher、sseBus
- `app/start.sh` 并行起 18080(后端)+ 15173(前端)
- `luna6` 数据分支 + git worktree(`/Users/xubinfeng/repos/pmd-instances/luna6`),最小种子(1 PDT + 1 LTC + 2 模块)
- 39 个 role_matrix 测试

### Phase 2 — 管理后台 CRUD(commit `d240050`–`cfce8e3`)
- 后端:`POST /api/seed/demo`、`GET /api/users/search` 加 limit、时间戳升级 μs、422/403 顺序修正
- 前端复用 oversea_projects:`UserSearchInput.vue` × 2 + `useContactCache.js`
- 前端 harness:`ConfirmDialog` / `InlineEdit` / `DraggableList`
- 前端 admin 五件套:`PdtForm` / `LtcList` / `ModuleEditor` / `AdminUsers` / `SnapshotPanel`
- 顶栏「管理」条件可见 + 「+ 示例数据」按钮
- 53 个后端测试全绿(39 role_matrix + 14 CRUD)
- design 10/11/12/13 已回写

### Phase 3 — 飞书 OAuth 真凭证接入(2026-05-22,阶段重排后插入)
- 复用 lark-cli 现有应用 `cli_a943bae284f89cd1`,凭证走 `LARK_APP_ID`/`LARK_APP_SECRET` 环境变量(回退别名)
- 后端 `/api/feishu/login_url` + `/api/feishu/callback` 实现完整:app_access_token → code 换 user_access_token → user_info → tenant 白名单 → upsert user_registry → 签 JWT
- 前端 `useFeishuLogin.js` + App.vue 登录闸 + `/feishu/callback` 路径处理
- `/api/auth/me` 增加 `feishu_configured` 字段
- design 03/11 已回写
- 待用户在飞书开发者后台为 `cli_a943bae284f89cd1` 加 `http://localhost:15173/feishu/callback` 重定向 URL + 核对 scope,才能真实走通(详见 `系统/飞书app申请清单.md`)

---

## 下一步

### Phase 4 — 状态填报编辑流 + 历史周只读 UI(原 Phase 3,顺延)

目标:让 Module Owner 在 PDT 总览页 / LTC 进展页 / 风险详情页**直接点击色块编辑**;历史周下所有编辑入口禁用并显示只读 banner。

**前端**
- `StatusCell.vue` 编辑态:点击 → 弹出小面板,选色 + 填 risk_note(`module_color != green` 时必填)+ 可选改 KPI 值 → 保存 = `PUT /api/status/{module_id}`
- 子项色块独立编辑(整模块灯与子项灯都可改)
- `useDashboard.isReadonly` 已就位,需要把它传透到所有 `StatusCell` 的 `editable` prop
- `useEditableModules` composable:从 `/api/auth/me` + `/api/modules` + `/api/admins` 算出"我能编辑哪些 module_id"集合,组件按 id 取 boolean
- 错误处理:422 风险说明缺失时 inline 提示;403 时 toast

**后端**
- 已实现 `PUT /api/status/{id}` 双写事务,无需新端点
- 可能需要补:`GET /api/me/editable_modules`(可选,前端也能从已有 4 个端点算出来)

**测试**
- 前端 vitest:StatusCell 编辑态、必填校验、权限禁用
- 后端 role_matrix 已覆盖

**design 回写**
- `design/13` §3.3 / §4.3 / §5.3 的"编辑入口"细化
- `design/07` 风险说明必填条件的前端兜底校验位置

---

## 后续阶段(顺序按 `design/02` 推进)

### Phase 5 — 三大查看页打磨
- PDT 总览页 KPI 展示对齐 `datas/` 截图风格
- LTC 进展页:模块整体灯与子项不一致时显示黄色三角警告
- 风险详情页:大字号、便于评审会议大屏
- TimelineBar 视觉对齐

### Phase 6 — 周快照前端联动
- WeekSwitcher 已就位,补 `useDashboard.loadSnapshot` 切换流
- 历史周 banner 与编辑禁用(本质在 Phase 4 已经预埋)
- SnapshotPanel 链接已就位

### Phase 7 — 定时周快照
- 后端 cron(uvicorn 内 `asyncio` 定时 / 或独立 systemd timer 调 `start-instance.sh`)
- 每周五 18:00 自动 freeze
- 实例配置 `auto_freeze_enabled`

### Phase 8 — 视觉验收 + 部署
- Playwright 走完 4 个视图截图,与 `datas/` 对比
- 写 `系统/部署流程.md`
- main 分支 hook 拦截非空 data 提交(`03-代码与数据分离约束` §8)

---

## 长期 TBD(本文档不深入,记录在此免遗忘)

- 评论与 @ 提醒(不在 v1)
- 导出 PPT/PDF 周报(不在 v1)
- 多 PDT 聚合视图(超出单实例范围,见 `14` §4.2)
- 数据库化(JSON → SQLite/Postgres,见 `14` §4.3)
- 移动端响应式

---

## 更新规则

- 每完成一个 Phase,**先**更新本文件"已完成"+"下一步",再 commit
- "下一步"区只放一个 Phase 的展开;后续阶段只列标题
- 长期 TBD 不放细节,展开放在 `design/14`
