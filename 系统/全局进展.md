# PMD 全局进展

> 单一事实来源:当前做到哪里、下一步做什么。所有阶段推进必须先更新本文档,再动代码。

最后更新:2026-05-22(Phase 8 完成)

---

## 待用户动作(阻塞下一步)

- [ ] 在 https://open.feishu.cn 给应用 `cli_a943bae284f89cd1`(复用 lark-cli 现有 app)添加重定向 URL `http://localhost:15173/feishu/callback`
- [ ] 核对该 app 的 scope 含 `contact:user.base:readonly` 等(详见 `系统/飞书app申请清单.md`)

完成后 `DEV_LOGIN=0 ./app/start.sh` → 浏览器访问 `http://localhost:15173/` 可走真实飞书登录。

---

## 已完成

### Phase 0 — 文档体系(commit `29594d7`)
- 项目根 `CLAUDE.md` + harness 工作流
- `design/01`–`14` 共 14 篇约束文档(含 `03-代码与数据分离` 单独承载)
- `design/CLAUDE.md` / `design/_index.md` / `app/*/CLAUDE.md` 路由

### Phase 1 — 骨架(commit `a99ea94`, `9f128fd`)
- 后端 FastAPI 单文件 `main.py`:文件锁原子写、AuthMiddleware、Dev 后门、五角色判定、PDT/LTC/Module CRUD、status 双写、SSE、周快照
- 前端 Vue 3 + Vite:`?view=` 路由、4 个视图占位、StatusCell、TimelineBar、WeekSwitcher、sseBus
- `app/start.sh` 并行起 18080(后端)+ 15173(前端)
- `luna6` 数据分支 + git worktree(`/Users/xubinfeng/repos/pmd-instances/luna6`),最小种子(1 PDT + 1 LTC + 2 模块)
- 39 个 role_matrix 测试

### Phase 2 — 管理后台 CRUD(commit `d240050`–`cfce8e3`)
- 后端:`POST /api/seed/demo`、`GET /api/users/search` 加 limit、时间戳升级 μs、422/403 顺序修正
- 前端复用 oversea_projects:`UserSearchInput.vue` × 2 + `useContactCache.js`
- 前端 harness:`ConfirmDialog` / `InlineEdit` / `DraggableList`
- 前端 admin 五件套:`PdtForm` / `LtcList` / `ModuleEditor` / `AdminUsers` / `SnapshotPanel`
- 顶栏「管理」条件可见 + 「+ 示例数据」按钮
- 53 个后端测试全绿(39 role_matrix + 14 CRUD)
- design 10/11/12/13 已回写

### Phase 3 — 飞书 OAuth 真凭证接入(commit `1263c25`)
> 阶段重排说明:原 Phase 6 提前到此位置,因为后续功能要在真实登录态下验收;原 Phase 3 顺延为 Phase 4。

- 复用 lark-cli 现有应用 `cli_a943bae284f89cd1`,凭证走环境变量 `FEISHU_APP_ID`/`SECRET`,回退别名 `LARK_APP_ID`/`SECRET`
- 后端 `/api/feishu/login_url` + `/api/feishu/callback`:app_access_token → code 换 user_access_token → user_info → tenant 白名单 → upsert user_registry → 签 JWT 写 cookie
- `/api/auth/me` 增 `feishu_configured` 字段
- 前端 `useFeishuLogin.js` + `App.vue` 登录闸(未登录且配置就绪 → 显示"飞书登录"按钮)
- design `03 §4.2` / `11 §6.1` 已回写
- 58 个后端测试全绿(原 53 + 新增 5 飞书 OAuth)
- 真实浏览器端到端验证 = 待用户完成上方"待用户动作"

### Phase 4 — 状态填报编辑流 + 历史周只读 UI
- 新增 `useAdmins`(admins 单例)与 `useEditableModules`(镜像后端 `can_edit_module_status` 的 canEdit/editableSet)
- 新增 `StatusEditDialog.vue`:选色 + 子项色 + KPI + risk_note 一次性 `PUT /api/status/{id}`;非绿必填前端兜底 + 422 inline + 403 toast
- `PdtOverview` / `LtcProgress` / `RiskDetail` 接入 `canEdit` + 弹窗;LtcProgress 新增"绿/子项非绿"⚠ 不一致提示
- 历史周 banner 已就位;`editableSet` 在 `isReadonly` 下强制为空
- 11 个前端 vitest 用例全绿(useEditableModules × 5 / StatusCell × 2 / StatusEditDialog × 4)
- 58 个后端测试保持全绿
- design 13 §3.3 / §4.3 / §5.3 + design 07 §5.5 已回写

### Phase 5 — 视觉打磨(借鉴 oversea_projects 风格,守 6px 圆角红线)
- `style.css` 全局 token 重写:借鉴 oversea 清透专业风(主色 `#4f6ef7`、文字 `#1a1f36`/`#5e6c84`、shadow-sm/md/lg、状态色补 `*-bg` 半透明背景版)
- 滚动条、聚焦圆形阴影、按钮 hover transition 等微交互到位
- `App.vue` 顶栏:sticky + backdrop-blur、品牌"📊 PMD"加渐变文字、用户身份做软背景 chip
- `TimelineBar`:横轴 + 类型徽章 + dot 阴影圈;过去/即将到期/未来三态视觉区分
- `PdtOverview`:卡片 shadow-sm + hover、KPI 缺失占位 `— —`、风险说明改 2 行 clamp + 黄底
- `LtcProgress`:列加软背景容器、模块卡 shadow、子项色块 grid 布局
- `RiskDetail`:整体字号放大(模块名 17px / 风险说明 15px / 标题 26px),「本周无风险 🎉」改大字绿底
- Playwright 基线截图 → 改样 → 回归截图,4 视图渲染正常,编辑弹窗显示完整
- 11 个前端 vitest 用例保持全绿
- design 12 §UI规范保持不变(仍 6px / 状态色变量 / 严禁胶囊)

### Phase 6 — 周快照前端联动
- `useView` 增加 `week` 参数,正则校验 `YYYY-Www`;`pushView` 同步写入 URL
- `App.vue` `watch(current.week)` → `useDashboard.setWeek`,URL 成为周次的单一事实来源
- 顶栏 nav 切换视图保留 `week`(不会意外退回当前周)
- `WeekSwitcher` 改为读 URL 写 URL,订阅 SSE `snapshot:created` 自动刷新下拉
- `SnapshotPanel` 高亮当前查看周(`正在查看` 徽章 + 行底色)、内联「查看」按钮 / 「返回当前周」按钮、`snapshot:created` 自动刷新表格
- Playwright 验证:管理后台冻结 → 切换历史周 → 顶栏 banner 出现 → 编辑入口消失 → 切换视图 URL 保留 week
- 11 个前端 vitest 用例保持全绿
- design 09 §7 + design 13 §7 已回写

### Phase 7 — 定时周快照
- 后端提炼 `_freeze_snapshot(week, frozen_by, trigger, force)` 复用函数,手动与定时共用
- FastAPI `@app.on_event("startup")` 启 `asyncio.Task` 每 60s 巡检;`PMD_DISABLE_SCHEDULER=1` 可关
- 新增 `GET /api/config/auto_freeze`(任意登录角色可读,返回 `enabled/weekday/hour/minute/next_run_at`)+ `PUT /api/config/auto_freeze`(仅 Super,422 范围校验)
- 配置持久化在 `config.json.auto_freeze`,新加「config」文件锁
- SnapshotPanel 新增"自动冻结"配置块:开关 + weekday/hour/minute 输入 + 下次执行预览;Super 可改,其他角色 read-only
- 75 个后端测试全绿(原 58 + role_matrix 新增 2 行 + test_auto_freeze 新增 7 用例)
- 11 个前端 vitest 用例保持全绿
- design 09 §6.1 / design 10 §5 / design 11 §8 已回写

---

### Phase 8 — 视觉验收 + 部署
- Playwright 4 视图截图(`phase8-0{1..4}-*.png` 在 repo 根):PDT 总览 / LTC 进展 / 风险详情 / 管理 全部正常渲染;TR/SOP 里程碑徽章、状态色块、子项色块均守 6px;路由 nav 使用 `view=risks`(复数)
- `系统/部署流程.md` 落地:目录布局 / env / systemd unit / Nginx 反代 / 首启动校验清单 / 升级 / 备份 / 回滚
- `.githooks/pre-commit` 落地:main 分支拦截 `design/*.json|*.jsonl|weekly_snapshots/`;通过 `git config core.hooksPath .githooks` 启用,本仓已生效;实测对暂存 `design/pdt.json` 返回 exit=1
- design 03 §8 已回写(钩子已实现 + validate_data_files.py 标 TBD)
- 75 个后端测试 + 11 个前端 vitest 保持全绿(本阶段未触代码逻辑)

---

## 下一步

### Phase 9 — 真实 Luna 6 实例数据落地(待用户解锁)
- 阻塞:`系统/全局进展.md` 顶部"待用户动作"中的飞书 app 重定向 URL + scope 核对
- 解锁后:把 `datas/luna6-seed.md` 提炼为 data-luna6 分支的真实 PDT/LTC/modules
- 跑 Phase 3 真实登录端到端验收
- 补 `design/validate_data_files.py`(03 §8 TBD)

---

## 长期 TBD(本文档不深入,记录在此免遗忘)

- 评论与 @ 提醒(不在 v1)
- 导出 PPT/PDF 周报(不在 v1)
- 多 PDT 聚合视图(超出单实例范围,见 `14` §4.2)
- 数据库化(JSON → SQLite/Postgres,见 `14` §4.3)
- 移动端响应式
- Phase 3 飞书 app 升级:dev 期复用 lark-cli app(`cli_a943bae284f89cd1`),正式演示前另建独立 PMD app(名字/图标专属)

---

## 更新规则

- 每完成一个 Phase,**先**更新本文件"已完成"+"下一步",再 commit
- "下一步"区只放一个 Phase 的展开;后续阶段只列标题
- 长期 TBD 不放细节,展开放在 `design/14`
